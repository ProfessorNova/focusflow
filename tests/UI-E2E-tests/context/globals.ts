// Not needed
import { type Browser, type BrowserContext, type Page } from "playwright/test";

// Playwright context
export let browser: Browser | null;
export let context: BrowserContext | null;
export let page: Page | null;